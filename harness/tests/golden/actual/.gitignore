# Frames written here by a failing golden_frame_test run, so the failure can be
# inspected as a picture (`disasm imgdiff ../<machine>.png <machine>.png`).
# Diagnostic output, never committed — the reference PNGs one level up are.
*.png
# Movies recorded by the movie-entry mechanism test.
*.phmi
