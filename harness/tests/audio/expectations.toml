# What each machine's audio is allowed to look like.
#
# `audio_sanity_test.rs` runs every registered machine, captures its audio, and
# asserts three things that need no reference capture and no MAME: the output is
# not on a large DC offset, is not permanently saturated, and is not permanently
# silent. The audio counterpart of `golden_frame_test`'s guards.
#
# THE FIXTURE MATTERS MORE THAN THE THRESHOLDS. The suite replays a recorded
# input movie when one exists and falls back to an input-free boot when none
# does, and it only judges *silence* on a movie. An input-free boot sits in
# attract mode, where most arcade boards are legitimately silent until coined
# up, so calling that silence a defect measures the fixture rather than the
# machine. This file previously carried 23 machines listed as silent for exactly
# that reason; every one of them turned out to produce healthy audio under
# replay, and the ratchet below is what forced the correction.
#
# There are two kinds of entry, and they mean opposite things.
#
#   [[correct]]        this machine really is like that, and here is why.
#   [[known_defect]]   this machine is broken, and here is the issue tracking it.
#
# `known_defect` is a RATCHET, not a skip list: the test asserts such a machine
# is *still* defective. Fixing one fails the suite until its entry is deleted,
# so the list can only shrink and cannot quietly absorb a new regression.
#
# A machine with no entry must pass the defaults. Registering a new machine
# needs no entry here unless it fails.

[defaults]
# Mean sample as a fraction of full scale. A large offset means a stuck source,
# a wrong bias, or a missing coupling capacitor, and it eats the headroom every
# real voice needs.
max_dc_offset = 0.05
# Fraction of samples pinned at the rail.
max_clipped_fraction = 0.01
# Fraction of a *movie* capture that may be silent. Loose by design — this
# catches a machine that emits nothing during recorded play, not one with quiet
# passages.
max_silent_fraction = 0.999

# ---------------------------------------------------------------------------
# Known defect: DC offset and saturation
# ---------------------------------------------------------------------------
# Measured 2026-08-19. Machines with a movie are measured over recorded play;
# the rest over a ten-second input-free boot, noted per entry. Both fixtures see
# a pinned or offset output, so unlike silence these findings do not depend on
# which one was used — though the magnitudes do, which is why the fixture is
# recorded alongside the number.

[[known_defect]]
machine = "ccastles"
dc = true
clipping = true
reason = "movie: DC -0.966 with 67.9% clipped — the worst of the set, and badly saturated during real play"
issue = "phosphor-emulator-audio-dc-play-sweep-stxr"

# Note joust and robotron keep only `clipping` — the Williams coupling
# capacitor cleared their offsets (joust -0.350 -> +0.001, robotron +0.074 ->
# -0.002) but both still drive the rail, which is excess gain rather than bias.
# Sinistar came clean on both counts and its entry is gone.

[[known_defect]]
machine = "qbert"
dc = true
clipping = true
reason = "movie: DC -0.430 with 4.0% clipped"
issue = "phosphor-emulator-audio-dc-play-sweep-stxr"

[[known_defect]]
machine = "joust"
clipping = true
reason = "movie: 3.9% clipped with DC now clean (+0.001) — residual excess gain, not bias"
issue = "phosphor-emulator-audio-dc-play-sweep-stxr"

[[known_defect]]
machine = "burgertime"
dc = true
reason = "movie: DC +0.198, no clipping — an offset riding on otherwise healthy audio (peak -2.0 dBFS)"
issue = "phosphor-emulator-audio-dc-play-sweep-stxr"

[[known_defect]]
machine = "dkong"
dc = true
reason = "movie: DC -0.136, essentially no clipping; the discrete sound path is the first fitting target"
issue = "phosphor-emulator-audio-dc-play-sweep-stxr"

[[known_defect]]
machine = "docastle"
dc = true
reason = "movie: DC +0.132 with 0.4% clipped"
issue = "phosphor-emulator-audio-dc-play-sweep-stxr"

[[known_defect]]
machine = "robotron"
clipping = true
reason = "movie: 15.8% clipped with DC now clean (-0.002) — the loudest of the Williams set and the clearest case of excess gain"
issue = "phosphor-emulator-audio-dc-play-sweep-stxr"

[[known_defect]]
machine = "dorunrun"
dc = true
reason = "boot (no movie): DC offset over threshold; shares the Do's Castle hardware with docastle and dowild, which show the same"
issue = "phosphor-emulator-audio-dc-play-sweep-stxr"

[[known_defect]]
machine = "dowild"
dc = true
reason = "boot (no movie): DC offset over threshold; shares the Do's Castle hardware with docastle and dorunrun, which show the same"
issue = "phosphor-emulator-audio-dc-play-sweep-stxr"

[[known_defect]]
machine = "mariobros"
dc = true
clipping = true
reason = "boot: DC -1.0 with 100% clipped; its movie was recorded against a different ROM name list so play could not be measured (…-movie-rom-digest-names-not-contents-aa1c)"
issue = "phosphor-emulator-audio-dc-play-sweep-stxr"
